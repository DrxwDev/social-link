import Card from "./Card";

export default function UserCard() {
  return (
    <div className="h-screen max-w-screen flex flex-col items-center justify-center bg-[var(--customGrey900)]">
      <Card />
    </div>
  );
}
